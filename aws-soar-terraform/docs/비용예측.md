# 예상 비용표 · 팀 승인 기록

학생가이드 4장(비용 예측·팀 승인·실행 절차) 제출용.
**모든 금액은 추정치입니다.** apply 전에 AWS Pricing Calculator로 다시 확인하고,
plan 결과의 실제 생성 자원과 대조한 뒤 팀 최종 승인을 받습니다.

- 리전: `ap-northeast-2` (서울)
- 기준 단가 확인일: ____________ ← **채워 주세요**
- 작성자 / 실행 담당자: ____________

---

## 1. 자원 목록과 시간당 추정 비용

기본값 기준: `instance_type = t3.micro`, `enable_vpc_endpoints = true`, ALB·NAT·공격자 off

| 자원 | 수량 | 단가(추정) | 시간당 | 비고 |
|---|---:|---|---:|---|
| EC2 t3.micro (web_dvwa/db/docker_host/dashboard) | 4 | $0.0144/h | $0.0576 | `attacker` 는 기본 off |
| EBS gp3 root | 4 | $0.0912/GB·월 | $0.0041 | |
| 퍼블릭 IPv4 (퍼블릭 서브넷 인스턴스) | 2 | $0.005/h | $0.0100 | 2024년부터 유료 |
| **VPC 인터페이스 엔드포인트** (ssm/ssmmessages/ec2messages) | 3 | $0.011/h | $0.0330 | **기본 on** |
| **소계 — 항상 켜짐** | | | **$0.1047** | **약 하루 $2.51** |
| 공격자 EC2 (`enable_attacker_instance`) | 0~1 | $0.0144/h | $0.0144 | **기본 off** |
| NAT Gateway (`enable_nat_gateway`) | 0~1 | $0.059/h + $0.062/GB | $0.0590 | **기본 off** |
| ALB (`enable_alb`) | 0~1 | $0.0225/h + LCU | $0.0225 | **기본 off** |
| WAF WebACL (`enable_waf`) | 0~1 | $5/월 + 룰 $1/월×2 | $0.0096 | ALB 켤 때만 |
| ECR 저장소 | 1 | $0.10/GB·월 | 소액 | 수명주기 정책으로 정리 |

> VPC 엔드포인트가 기본 on 인 이유는 NAT 대신 Session Manager 경로를 여는 것이라서입니다.
> NAT($0.059/h)보다 엔드포인트 3개($0.033/h)가 쌉니다. 다만 **일반 인터넷 접근이 필요하면
> NAT가 별도로 필요**합니다. 둘 다 켜면 비용이 겹칩니다.

### 사용량 기반 (시간 비례 아님)

| 서비스 | 과금 방식 | 이 프로젝트 예상 | 비고 |
|---|---|---|---|
| GuardDuty | CloudTrail 이벤트 100만건당 + VPC Flow/DNS GB당 | 소액 | 30일 무료 체험 |
| Inspector v2 | EC2 인스턴스당 월 과금 | 5대분 | 15일 무료 체험 |
| Security Hub | 보안 점검 1만건당 + finding 수집 | 소액 | 30일 무료 |
| AWS Config | 구성항목당 + 규칙 평가당 | 규칙 4개 | 변경 잦으면 증가 |
| CloudTrail | 관리 이벤트 첫 번째 추적은 무료 | $0 | S3 저장료는 별도 |
| CloudWatch Logs | 수집 GB당 + 보관 GB·월 | Flow Logs 비중 큼 | **보존기간 확인** |
| VPC Flow Logs | CloudWatch 전송량 | 트래픽 비례 | |
| Lambda / DynamoDB / SNS | 호출·요청 기반 | 거의 $0 | 프리티어 내 |
| S3 (CloudTrail/Config) | 저장 GB·월 + 요청 | 소액 | 수명주기 규칙 있음 |
| KMS | 키당 월 $1 + 요청 | $1 | CloudTrail 암호화용 |

---

## 2. 누적 예산 산정

| 시나리오 | 계산 | 10일 합계(추정) |
|---|---|---|
| 최소 (토글 전부 off, 24h 가동) | $0.1047 × 240h | **약 $25.1** |
| NAT 포함 (초기 부트스트랩 몇 시간만) | + $0.059 × 6h | + 약 $0.4 |
| ALB + WAF 포함 (시연 2일만) | + $0.0321 × 48h | + 약 $1.5 |
| 탐지 서비스 사용량 | 체험판 범위 가정 | + $2 ~ $10 (변동 큼) |
| **합계** | | **약 $30 ~ $40** |

야간에 인스턴스를 중지하면 EC2분(약 60%)이 빠집니다. EBS·EIP는 중지해도 계속 과금됩니다.

**팀 총예산 한도:** ____________
**초과 시 조치:** ____________

---

## 3. 팀 사전 고지 (가이드 4.1)

apply 전에 아래를 팀에 공유하고 기록을 남기면 됩니다.

| 항목 | 내용 |
|---|---|
| 실행 예정 일시 | |
| 실행 담당자 | |
| 생성·변경·삭제될 자원 | `terraform plan` 요약을 첨부 |
| 예상 비용 / 누적 예산 | 위 2장 |
| 서비스 중단 가능성 | |
| Public IP 변경 가능성 | |
| DB 재생성 가능성 | `-/+ aws_instance.db` 이면 **데이터 손실** |
| 보안 영향 (SG/IAM/Public IP) | |
| 백업 여부 | |
| 실패 시 롤백 방법 | |
| 종료 시 삭제 계획 | `terraform destroy` + 잔여 자원 확인 |

### 승인 기록

| 일시 | 안건 | 참석자 | 결과 | 비고 |
|---|---|---|---|---|
| | 1차 승인 (코드 초안 + 비용 예측) | | □ 승인 □ 보류 | |
| | 2차 승인 (plan 공유 + 비용 재확인) | | □ 승인 □ 보류 | |

---

## 4. 표준 실행 절차

```bash
terraform init
terraform fmt
terraform validate
terraform plan -out=tfplan   # ← 팀에 공유하고 비용 재확인
terraform apply tfplan       # ← 2차 승인 후에만
```

`-auto-approve` 는 쓰지 않습니다. plan에 `-/+ replace` 가 보이면 무엇이 지워지는지 먼저 확인하면 됩니다.

## 5. 종료 절차

```bash
terraform plan -destroy   # 삭제 대상 검토
terraform destroy         # 강사 확인 + 증적 백업 완료 후
```

destroy 후 콘솔에서 직접 확인해 주세요 — S3 버킷(객체 남으면 삭제 실패),
CloudWatch Log Group, KMS 키(삭제 대기), Elastic IP, Inspector/GuardDuty 활성화 상태.
