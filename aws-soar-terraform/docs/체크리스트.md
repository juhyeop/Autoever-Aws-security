# 최종 제출 체크리스트

학생가이드 12장의 16개 항목 + 이 프로젝트의 현재 상태.

| 영역 | 필수 확인 항목 | 현재 | 확인 |
|---|---|---|:---:|
| Cost | apply 전 예상 비용·실행기간·누적 예산 산정 | `docs/비용예측.md` 작성, **값을 채워야 합니다** | □ |
| Team Approval | 변경 자원·비용·영향 사전 고지 + 승인 기록 | 양식만 있음, **기록이 필요합니다** | □ |
| Terraform | `fmt`·`validate` 성공, `plan` 공유·검토 | | □ |
| State/Secret | tfstate·tfvars·PEM·AWS Key·DB PW Git 제외 | ✅ `.gitignore` 정비 완료 | □ |
| Network | SSH 0.0.0.0/0 제거, MySQL 3306·Flask 5000 외부 차단 | ✅ SSH 규칙 자체 없음, 3306·5000 내부 전용 | □ |
| SG | 계층별 SG 참조와 최소 포트 | ✅ `referenced_security_group_id` 사용 | □ |
| EC2/Storage | IMDSv2, EBS 암호화, Public IP 필요성 검토 | ✅ 5대 전부 적용 | □ |
| IAM | Access Key 대신 Role, 과도한 권한 제거 | ✅ 역할 3종 + SSM. AdministratorAccess 미사용 | □ |
| Web/TLS | HTTPS, TLS 종료 지점, 서버 정보 노출 | ⚠️ 코드는 준비됨. ACM 인증서 없어 비활성 | □ |
| DB | 최소권한 계정, Public IP·root 원격접속·백업 | ✅ Private 서브넷 + Secrets Manager / 🔜 계정 권한 점검 | □ |
| Docker/K8s | 취약 이미지, root/privileged, RBAC/SA, NodePort | 🔜 `SCAN-ContainerImage` 플레이북 활용 | □ |
| Logging | CloudTrail·CloudWatch, 필요 시 Flow Logs | ✅ 셋 다 + Config·GuardDuty | □ |
| Evidence | Before, 변경 코드, Plan/Apply, After | `docs/증적양식.md` 양식만. **채워야 함** | □ |
| Function | 차단 테스트 + 정상 기능 테스트 **둘 다** 성공 | `demo/` 확장 필요 | □ |
| Git | 의미 있는 Commit 메시지와 변경 이력 | | □ |
| Cost | 발표·검수 후 비용 발생 자원 제거 확인 | `terraform destroy` + 콘솔 잔여 확인 | □ |

---

## 발표 필수 화면 9개 (가이드 10일차)

| # | 화면 | 준비 |
|---:|---|---|
| ① | 전체 아키텍처 | `설계문서/` 다이어그램 | □ |
| ② | Terraform 디렉터리 구조 | `README.md` 모듈 표 | □ |
| ③ | 주요 SG 코드 | `modules/network/security_groups.tf` | □ |
| ④ | 취약점 Before | 증적양식 5번 | □ |
| ⑤ | 코드 변경 Diff | `git diff` | □ |
| ⑥ | Plan·Apply 결과 | | □ |
| ⑦ | 취약점 After | 증적양식 11번 | □ |
| ⑧ | 정상 기능 | 증적양식 12번 | □ |
| ⑨ | Git 변경 이력 | `git log --oneline` | □ |

---

## 잔여 위험 (발표에서 먼저 언급하면 좋습니다)

숨기면 감점이고, 먼저 밝히면 가점입니다.

1. **HTTPS 미적용** — ALB/NLB TLS 리스너와 ACM 인증서 미구성. HTTP 평문 전송.
2. **ALB가 0.0.0.0/0** — 뒷단이 DVWA입니다. 실습 중에는 `admin_cidr` 로 좁히는 것을 권장합니다.
3. **로컬 state** — `backend.tf.example` 상태. 팀 동시 작업 시 충돌 위험.
4. **DVWA는 의도적으로 취약** — 공격 시연 대상이므로 개선 대상이 아님. 단, `admin_cidr` 밖에서 접근 불가해야 함.
5. **NAT Gateway 기본 off** — VPC 엔드포인트로 SSM은 되지만, 패키지 설치엔 NAT가 필요합니다.

---

## 금지사항 (가이드 11장)

- tfstate / PEM / AWS Key / DB Password 공개 저장 금지
- Secret을 로그·스크린샷에 노출 금지
- SSH·MySQL 장기간 `0.0.0.0/0` 금지
- `terraform apply -auto-approve` 금지, plan 미확인 apply 금지
- 타인·운영 자산 스캔 금지, 허가 없는 부하 테스트 금지
