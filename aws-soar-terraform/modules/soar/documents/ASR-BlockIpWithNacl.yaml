schemaVersion: '0.3'
description: |
  공격 출발지 IP 를 Private NACL 의 Deny 규칙(번호 1~99)으로 차단합니다 (SEC-06).
  오탐 가능성 때문에 자동화하지 않고, 담당자가 대시보드에서 승인한 뒤 실행합니다.
  before/after 규칙을 반환해 Before-After 비교에 사용합니다.
assumeRole: '{{ AutomationAssumeRole }}'
parameters:
  NetworkAclId:
    type: String
    description: 규칙을 추가할 NACL ID (private NACL)
  AttackerCidr:
    type: String
    description: "차단할 출발지 CIDR (예: 10.0.2.55/32)"
  RuleNumber:
    type: Integer
    default: 10
    description: Deny 규칙 번호 (1~99, Allow 100 보다 앞)
  AutomationAssumeRole:
    type: String
mainSteps:
  - name: addDenyRule
    action: 'aws:executeScript'
    outputs:
      - Name: before
        Selector: $.Payload.before
        Type: String
      - Name: after
        Selector: $.Payload.after
        Type: String
    inputs:
      Runtime: python3.11
      Handler: handler
      InputPayload:
        nacl_id: '{{ NetworkAclId }}'
        cidr: '{{ AttackerCidr }}'
        rule_number: '{{ RuleNumber }}'
      Script: |
        import boto3, json
        def _entries(ec2, nacl_id):
            acl = ec2.describe_network_acls(NetworkAclIds=[nacl_id])["NetworkAcls"][0]
            return [e for e in acl["Entries"] if not e["Egress"]]
        def handler(event, context):
            ec2 = boto3.client("ec2")
            nacl_id = event["nacl_id"]
            before = json.dumps(_entries(ec2, nacl_id), default=str)
            ec2.create_network_acl_entry(
                NetworkAclId=nacl_id, RuleNumber=int(event["rule_number"]),
                Protocol="-1", RuleAction="deny", Egress=False,
                CidrBlock=event["cidr"])
            after = json.dumps(_entries(ec2, nacl_id), default=str)
            return {"before": before, "after": after}
